json.extract! @reservation, :id, :user_id, :restaurant_id, :time, :date, :num_of_guests, :restaurant, :user
json.photo_url @reservation.restaurant.photo.attached? ? @reservation.restaurant.photo.url : nil